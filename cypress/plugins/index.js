/* eslint-disable unicorn/prefer-module */

/**
 * @type {Cypress.PluginConfig}
 */
module.exports = (_, config) => {
  /**
   * Here you can do things like set Cypress.env variables like:
   * config.env.FOO = 'BAZ'
   */

  return config
}
